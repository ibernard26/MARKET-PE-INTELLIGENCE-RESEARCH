node_modules/
dist/
*.log
.claude/settings.local.json
tmp/
.mcp.json


# delivered artifact, not source (sent to user via chat)

# secrets — never commit (FRED_API_KEY, SEC_USER_AGENT contact, etc.)
*.env
.env.*
!.env.example
!**/.env.example
secrets.*
*.secret
